// import { connectToDatabase, CounselingContact } from '@lumora/db';
import { Contact } from '@lumora/core';

export class ResourcesService {
  // async getCounselingContacts(): Promise<Contact[]> {
    // await connectToDatabase();
    // const contacts = await CounselingContact.find({});
    // return contacts.map(contact => ({
    //   name: contact.name,
    //   phone: contact.phone,
    //   email: contact.email,
    //   hours: contact.hours,
    //   locationUrl: contact.locationUrl,
    // }));
  // }
}





